{
  "name": "${project_name}",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "test": "postdoc test",
    "start": "postdoc run",
    "build": "postdoc build"
  },
  "devDependencies": {
    "shiki": "${shiki_version}",
    "postdoc": "${postdoc_version}",
    "nightwatch": "${nightwatch_version}",
    "geckodriver": "${geckodriver_version}"
  }
}